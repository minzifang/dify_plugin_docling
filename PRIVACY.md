# Privacy Policy

This plugin sends uploaded files to the Docling API service configured by the Dify workspace administrator. Files are stored temporarily by the plugin only during a single tool invocation.

The plugin itself does not intentionally collect or persist user documents. Data handling by the configured Docling API service depends on that service's deployment, logging, storage, and retention configuration.
